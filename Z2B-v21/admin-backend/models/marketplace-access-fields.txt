
    // Marketplace Product Access Management (ALL Products)
    marketplaceAccess: {
        mavula: {
            hasAccess: { type: Boolean, default: false },
            accessType: { type: String, enum: ['PAID', 'GIFT', 'BETA', 'ADMIN', 'FREE'] },
            subscriptionPlan: { type: String, enum: ['MONTHLY', 'LIFETIME', 'PAY_AS_GO'] },
            grantedDate: { type: Date },
            expiryDate: { type: Date },
            grantedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            notes: { type: String },
            revokedDate: { type: Date },
            revokedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            revocationReason: { type: String }
        },
        zyro: {
            hasAccess: { type: Boolean, default: false },
            accessType: { type: String, enum: ['PAID', 'GIFT', 'BETA', 'ADMIN', 'FREE'] },
            subscriptionPlan: { type: String, enum: ['MONTHLY', 'LIFETIME', 'PAY_AS_GO'] },
            grantedDate: { type: Date },
            expiryDate: { type: Date },
            grantedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            notes: { type: String },
            revokedDate: { type: Date },
            revokedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            revocationReason: { type: String }
        },
        zyra: {
            hasAccess: { type: Boolean, default: false },
            accessType: { type: String, enum: ['PAID', 'GIFT', 'BETA', 'ADMIN', 'FREE'] },
            subscriptionPlan: { type: String, enum: ['MONTHLY', 'LIFETIME', 'PAY_AS_GO'] },
            grantedDate: { type: Date },
            expiryDate: { type: Date },
            grantedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            notes: { type: String },
            revokedDate: { type: Date },
            revokedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            revocationReason: { type: String }
        },
        benown: {
            hasAccess: { type: Boolean, default: false },
            accessType: { type: String, enum: ['PAID', 'GIFT', 'BETA', 'ADMIN', 'FREE'] },
            subscriptionPlan: { type: String, enum: ['MONTHLY', 'LIFETIME', 'PAY_AS_GO'] },
            grantedDate: { type: Date },
            expiryDate: { type: Date },
            grantedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            notes: { type: String },
            revokedDate: { type: Date },
            revokedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            revocationReason: { type: String }
        },
        glowie: {
            hasAccess: { type: Boolean, default: false },
            accessType: { type: String, enum: ['PAID', 'GIFT', 'BETA', 'ADMIN', 'FREE'] },
            subscriptionPlan: { type: String, enum: ['MONTHLY', 'LIFETIME', 'PAY_AS_GO'] },
            grantedDate: { type: Date },
            expiryDate: { type: Date },
            grantedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            notes: { type: String },
            revokedDate: { type: Date },
            revokedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            revocationReason: { type: String }
        },
        vidzie: {
            hasAccess: { type: Boolean, default: false },
            accessType: { type: String, enum: ['PAID', 'GIFT', 'BETA', 'ADMIN', 'FREE'] },
            subscriptionPlan: { type: String, enum: ['MONTHLY', 'LIFETIME', 'PAY_AS_GO'] },
            grantedDate: { type: Date },
            expiryDate: { type: Date },
            grantedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            notes: { type: String },
            revokedDate: { type: Date },
            revokedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            revocationReason: { type: String }
        },
        zynth: {
            hasAccess: { type: Boolean, default: false },
            accessType: { type: String, enum: ['PAID', 'GIFT', 'BETA', 'ADMIN', 'FREE'] },
            subscriptionPlan: { type: String, enum: ['MONTHLY', 'LIFETIME', 'PAY_AS_GO'] },
            grantedDate: { type: Date },
            expiryDate: { type: Date },
            grantedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            notes: { type: String },
            revokedDate: { type: Date },
            revokedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            revocationReason: { type: String }
        },
        zynect: {
            hasAccess: { type: Boolean, default: false },
            accessType: { type: String, enum: ['PAID', 'GIFT', 'BETA', 'ADMIN', 'FREE'] },
            subscriptionPlan: { type: String, enum: ['MONTHLY', 'LIFETIME', 'PAY_AS_GO'] },
            grantedDate: { type: Date },
            expiryDate: { type: Date },
            grantedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            notes: { type: String },
            revokedDate: { type: Date },
            revokedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
            revocationReason: { type: String }
        }
    },
